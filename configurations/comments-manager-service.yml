spring:
  mongodb:
    uri: mongodb+srv://${DB_USER}:${DB_PASSWORD}@chilankash.wpeuqrw.mongodb.net/?appName=ChilankaSH
    database: comments-manager
  rabbitmq:
    username: csh_rabbit
    password: ${PASSWORD}

rabbitmq:
  queue:
    name: price.comments.queue
server:
  port: 8084

logging:
  file:
    name: comments-manager-service/comments-manager-service.log