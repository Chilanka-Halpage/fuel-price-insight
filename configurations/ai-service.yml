gemini:
  uri: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
  key: ${GEMINI_API_KEY}

logging:
  file:
    name: ai-service/logs/ai-service.

management:
  tracing:
    sampling:
      probability: 1.0