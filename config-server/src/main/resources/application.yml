spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Chilanka-Halpage/fuel-price-insight
          search-paths: configurations
server:
  port: 8888
